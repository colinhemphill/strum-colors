declare namespace oklch {
  {{#palette}}
  let {{colorKey}}: {
    category: 'accent' | 'accentBright' | 'neutral',
    colorName: string;
    dark: {
      {{#dark}}	
      {{shadeName}}: {
        color: string,
        foreground: string,
      },
      {{/dark}}
    },
    light: {
      {{#light}}	
      {{shadeName}}: {
        color: string,
        foreground: string,
      },
      {{/light}}
    }
  };
  {{/palette}}
}
declare namespace rgb {
  {{#palette}}
  let {{colorKey}}: {
    category: 'accent' | 'accentBright' | 'neutral';
    colorName: string;
    dark: {
      {{#dark}}	
      {{shadeName}}: {
        color: string,
        foreground: string,
      },
      {{/dark}}
    },
    light: {
      {{#light}}	
      {{shadeName}}: {
        color: string,
        foreground: string,
      },
      {{/light}}
    }
  };
  {{/palette}}
}
export { oklch, rgb };